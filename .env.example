# Server Configuration
PORT=3000
NODE_ENV=production

# MCP Server URL (REQUIRED)
MCP_SERVER_URL=https://your-google-ads-mcp.onrender.com/sse

# CORS Configuration (comma-separated origins, or * for all)
CORS_ORIGIN=*

# API Key Authentication (REQUIRED in production)
# Generate a secure random string using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY=your-secure-api-key-here
